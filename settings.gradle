pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// Library-only root. Example apps use their own settings.gradle with project(':vibes').
include ':vibes'
